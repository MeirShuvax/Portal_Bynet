# 🚀 מדריך הגדרת Render לשמירת תמונות

## ⚠️ הבעיה:
ב-Render, הקבצים בתיקיית השרת **נמחקים** בכל restart/redeploy!

## ✅ הפתרון: Render Persistent Disk

### **שלב 1: צור Persistent Disk**

1. לך ל-**Render Dashboard**: https://dashboard.render.com
2. בחר את ה-**Service** שלך (employee-portal-server)
3. לחץ על **Settings** (הגדרות)
4. גלול למטה ל-**Persistent Disks**
5. לחץ על **+ Add Persistent Disk**

### **שלב 2: הגדר את ה-Disk**

מלא את הפרטים:
- **Name**: `uploads-disk` (או כל שם שתרצה)
- **Mount Path**: `/opt/render/uploads` ⚠️ **חשוב!** זה הנתיב בתוך השרת
- **Size**: `10` GB (או כמה שאתה צריך)

לחץ **Save**

### **שלב 3: הוסף משתנה סביבה**

1. באותו Service, לך ל-**Environment** → **Environment Variables**
2. לחץ **+ Add Environment Variable**
3. הוסף:
   - **Key**: `UPLOADS_PATH`
   - **Value**: `/opt/render/uploads`
4. לחץ **Save Changes**

### **שלב 4: Redeploy**

1. לך ל-**Manual Deploy** → **Deploy latest commit**
2. או פשוט **Push** שינוי ל-GitHub (Render יעשה auto-deploy)

### **שלב 5: בדיקה**

1. העלה תמונה דרך האפליקציה
2. בדוק שהיא נגישה: `https://your-domain.com/uploads/filename.jpg`
3. עשה **Restart** לשרת (Settings → Restart)
4. בדוק שוב - התמונה עדיין צריכה להיות שם! ✅

---

## 📋 סיכום מה לעשות:

1. ✅ צור **Persistent Disk** ב-Render
   - Name: `uploads-disk`
   - Mount Path: `/opt/render/uploads`
   - Size: 10GB

2. ✅ הוסף משתנה סביבה:
   - Key: `UPLOADS_PATH`
   - Value: `/opt/render/uploads`

3. ✅ Redeploy את השרת

4. ✅ **זהו!** התמונות יישמרו באופן קבוע

---

## 💰 עלויות:

- **Persistent Disk**: ~$0.25/GB/חודש
- **10GB**: ~$2.50/חודש
- **20GB**: ~$5/חודש

---

## ❓ שאלות נפוצות:

**Q: מה קורה אם לא אצור Persistent Disk?**  
A: התמונות יאבדו בכל restart/redeploy! ❌

**Q: איך אני יודע כמה GB אני צריך?**  
A: התחל עם 10GB. אם תגיע ל-80%, Render יודיע לך.

**Q: איך אני משנה את הגודל?**  
A: Settings → Persistent Disks → Edit → שנה Size → Save

**Q: מה אם אני רוצה נתיב אחר?**  
A: אפשר, אבל `/opt/render/uploads` זה הנתיב המומלץ.

---

## 🔍 איך לבדוק שזה עובד:

```bash
# אחרי שיצרת את ה-Disk והוספת את המשתנה:
# 1. העלה תמונה
# 2. בדוק שהיא נגישה
# 3. עשה restart לשרת
# 4. בדוק שוב - התמונה עדיין צריכה להיות שם!
```

---

## 📝 הערות:

- **גיבוי**: גם עם Persistent Disk, מומלץ לגבות את התמונות
- **ניקוי**: מומלץ לנקות תמונות ישנות מדי פעם
- **גודל**: התמונות מוגבלות ל-5MB (ניתן לשנות)

---

## 🆘 בעיות נפוצות:

**התמונות עדיין נמחקות?**
- בדוק שה-Persistent Disk קיים
- בדוק ש-`UPLOADS_PATH` מוגדר נכון
- בדוק שה-Mount Path תואם ל-`UPLOADS_PATH`
- עשה restart לשרת

**לא יכול ליצור Persistent Disk?**
- וודא שאתה ב-**Paid Plan** (לא Free)
- Free Plan לא תומך ב-Persistent Disks

---

**✅ אחרי שתעשה את זה, התמונות יישמרו באופן קבוע!**

